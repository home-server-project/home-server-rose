# Ensure libc resolver traffic uses systemd-resolved so NetworkManager's
# default-route DNS and Tailscale MagicDNS can coexist through split DNS.
L+ /etc/resolv.conf - - - - /run/systemd/resolve/stub-resolv.conf
